Aplica esto siempre que leas o edites el archivo cloudbuild.yaml:

La indentación debe ser estrictamente de 2 espacios. Nunca uses tabulaciones.

Todo el código debe estar dentro de la clave principal steps:.

Cada paso debe usar una imagen base válida (por ejemplo, name: 'ubuntu').
